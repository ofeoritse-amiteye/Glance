/**
 * Public URL of your Glance proxy (no secrets here).
 * - Local dev: default below (start server: cd server && npm i && npm start)
 * - Production: set to your deployed HTTPS URL and reload the unpacked extension.
 */
// eslint-disable-next-line no-undef
self.GLANCE_PROXY_URL = 'http://127.0.0.1:3000/api/summarize'
